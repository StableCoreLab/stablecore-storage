# Task 6-4 / 6-5 结果确认单

## 1. backend 字段统一
- 统一为 `QueryBackendKind backendKind`
- 全仓库已移除 `backendType`
- 查询、测试、文档中的 backend 语义保持一致

## 2. ExecuteQueryPlan 冻结
- 唯一标准入口已固定为：
  - `ErrorCode ExecuteQueryPlan(const QueryPlan&, const QueryExecutionContext&, QueryExecutionResult*)`
- legacy 路径已全部桥接到该入口
- 不再保留第二套 public Execute 包装接口

## 3. Task 6-4: Fallback 扫描
- fallback 扫描已成为 backend executor 内部的正式执行路径
- SQLite / Memory 均支持 fallback 扫描
- 扫描模型具备：
  - 分批扫描
  - 分页输出
  - 统计 `scannedRows` / `matchedRows` / `returnedRows`
  - `fallbackTriggered` / `fallbackReason` / `fallbackSource`
- Core executor 不承担 fallback 扫描执行

## 4. Task 6-5: ReferenceIndex 只读接入
- `IReferenceIndexProvider` 已接入到 SQLite / Memory 数据库对象
- 提供：
  - `GetReferencesBySource`
  - `GetReferencesByTarget`
  - `CheckReferenceIndex`
  - `GetAllReferencesDiagnosticOnly`
- 删除前引用检查已收口为：
  - 先检查 ReferenceIndex 健康状态
  - 不可用时走 fallback scan
- 未引入任何 ReferenceIndex 写入、增量维护、重建逻辑

## 5. 结果可观测性
- `QueryExecutionResult` 已完整表达：
  - `mode`
  - `unsupportedSource`
  - `fallbackSource`
  - `fallbackReason`
  - `scannedRows`
  - `matchedRows`
  - `returnedRows`
  - `usedIndexIds`
  - `fallbackTriggered`
  - `executionNote`

## 6. 验证结果
- `SCStorageTests` 全量通过
- 当前总测试数：44
- 当前总失败数：0

